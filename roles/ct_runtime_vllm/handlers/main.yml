---
- name: Reload systemd
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart vllm
  ansible.builtin.service:
    name: "{{ ct_runtime_vllm_service_name }}"
    state: restarted
